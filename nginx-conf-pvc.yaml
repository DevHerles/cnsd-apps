apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-conf-pvc
  namespace: app-launcher
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs

